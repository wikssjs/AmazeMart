/** @type {import('next').NextConfig} */
export default {
  reactStrictMode: true,
  images: {
    domains: ['www.google.com','i5.walmartimages.com','raw.githubusercontent.com'], // Add the domain of the external image source
  },
}
